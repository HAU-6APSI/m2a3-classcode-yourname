// ───────────────────────────────────────────────────────────────────────────
// Counter - state and events
// ───────────────────────────────────────────────────────────────────────────
//
// Concept: the useState hook and event handling.
// Read: content/m2-react Activity 3, and
//       content/react-theory/04-hooks-mental-model.md
//
// Requirement:
//   Keep a count in state, starting at 0.
//   - a <button> labeled exactly "Increment" that adds 1 to the count
//   - a <button> labeled exactly "Decrement" that subtracts 1 from the count
//   - the current count shown as text in exactly this form:  Count: N
//
// Reminder: never reassign the state variable directly; call the setter the
// useState hook gives you, and pass onClick a function (not a function call).

import { useState } from 'react'

export default function Counter() {
  // TODO: declare state with useState, then return the buttons and the count.
  return null
}
